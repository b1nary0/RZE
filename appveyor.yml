version: 0.0.{build}
os: Visual Studio 2017

build:
  project: RZE/_Project/RZE.sln
  parallel: true
  verbosity: minimal
  
branches:
  only:
    - master
    - Sandbox
    - RenderRefactor
    
configuration:
 - Debug
 - Release
 
install:
  - cd RZE
  - mkdir _Artifacts
  - GenerateSolutions.bat
  - cd _Project
  
before_build:
  - cd ..
  - GenerateSolutions.bat
  - cd _Project
  
after_build:
  - xcopy /y /d %APPVEYOR_BUILD_FOLDER%\RZE\Engine\Assets _Artifacts
  - xcopy /y /d %APPVEYOR_BUILD_FOLDER%\RZE\_Build _Artifacts
  - 7z a RZE_Engine_Artifacts.zip RZE\_Artifacts
  
artifacts:
  - path: RZE/RZE_Engine_Artifacts.zip
    name: RZE_Engine_Artifacts